import 'package:flutter/material.dart';

class PoetsView extends StatelessWidget {

  const PoetsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(

      ),
    );
  }

}